#!/bin/bash

# Install packages
echo "Installing required dependencies..."
sudo pacman -S --needed --noconfirm \
    yadm \
    python \
    python-click > /dev/null

# Run bootstrap commands
python ~/.config/yadm/installer/main.py
